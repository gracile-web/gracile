// Local helper inside src/routes/ (underscore prefix = not a route, but importable).
// Touching this file should trigger a full SSR reload.

export const LOCAL_GREETING = 'Hello from local helper';
